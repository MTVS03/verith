"""Stock Resolver 매칭 상수 (boundary-aware).

전체 종목으로 확장돼도 오탐하지 않도록, 무제한 substring 대신 **token 경계** 기준으로 매칭한다.
token 끝에 붙는 아래 접미만 제거하고 비교한다(무공백 결합형 포함). 광의어("투자/대상/회사")는 넣지 않는다.
"""

from __future__ import annotations

# 한국어 조사 — token 끝에서 이것만 제거. 예: "LG화학은" → "LG화학", "LG화학과" → "LG화학".
JOSA_SUFFIXES: tuple[str, ...] = ("은", "는", "이", "가", "을", "를", "의", "에", "도", "과", "와")

# 명시적 종목 문맥 키워드 — 짧은 이름(≤2) 후보 인정 조건이자, 무공백 결합형 분리 접미.
# 예: "카카오주가" → "카카오" + "주가"(문맥).
CONTEXT_SUFFIXES: tuple[str, ...] = (
    "주가",
    "차트",
    "수급",
    "실적",
    "재무",
    "뉴스",
    "리포트",
    "분석",
    "전망",
)

# 짧은 공식명/alias 는 이 길이 이하일 때 보수 규칙 적용(문맥 필요).
SHORT_NAME_MAX_LEN = 2
